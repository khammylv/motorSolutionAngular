import { RemoveLettersPipe } from './remove-letters.pipe';

describe('RemoveLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
